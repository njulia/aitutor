# Core dependencies
fastapi
uvicorn[standard]
python-multipart
pydantic
chromadb
requests
stripe

# Local embedding model (zero API cost for RAG)
sentence-transformers

# Observability (self-hosted Langfuse)
langfuse>=2.0.0

# Optional but recommended
python-dotenv
jinja2

# For image handling
Pillow

# For document parsing
python-docx

# test
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
